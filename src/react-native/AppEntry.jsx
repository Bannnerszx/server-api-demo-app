import React from "react";
import { View, Text } from 'react-native'

const AppEntry = () => {
    return (
        <View style={{ justifyContent: 'center' }}>
            <Text style={{ fontSize: '2em' }}>Hello from client</Text>
        </View>
    )
}

export default AppEntry;